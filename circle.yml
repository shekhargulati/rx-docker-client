test:
  override:
    - ./circle.sh test:
      parallel: false
  post:
    - ./circle.sh post_test:
      parallel: false

machine:
  pre:
    - rx-docker-client/circle.sh pre_machine
  java:
    version: oraclejdk8
  services:
    - docker
